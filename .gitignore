# dependencies
/node_modules
/mobile/node_modules
/services/whatsapp-gateway/node_modules
/services/whatsapp-gateway/.env.local
# next / expo
/.next
/out
/mobile/.expo
/mobile/dist

# local configuration
.env*
!.env.example

# tooling
*.tsbuildinfo
coverage
# Playwright run artifacts; keep browser specs in tests/e2e tracked.
/playwright-report/
/test-results/
/blob-report/
.DS_Store
npm-debug.log*
pnpm-debug.log*

# local Supabase CLI link state
/supabase/.temp/
.vercel
.whatsapp-auth/
services/whatsapp-gateway/dist/


# local scratch scripts: they hold database URLs and demo passwords
/scratch/
# Trigger.dev local build output (bundled copies of the tasks)
/.trigger/
# one-off helper scripts written during test runs
*.tmp.mjs
